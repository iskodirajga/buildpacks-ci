---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci

inputs:
  - name: edge-ami
  - name: buildpacks-ci

run:
  path: buildpacks-ci/tasks/launch-ami/run.rb